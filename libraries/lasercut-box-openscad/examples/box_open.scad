include <box.scad>

box(
    width = 145,
    height = 135, 
    depth = 300,
    thickness = 4,
    hole_width = 100,
    hole_height = 20,
    hole_margin = 15,
    open = true,
    assemble = false
);
