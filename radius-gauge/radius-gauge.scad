// vim:ts=4:sw=4:sts=4:et:ai:si:fdm=marker
//
// Just some simple radius gauges

use <ymse.scad>

$fn = $preview ? 32 : 128;
bug = $preview ? .1 : 0;

min_r = 30;
max_r = 35;
step = 1;

module main() {
    for (
}

main();
