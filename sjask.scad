// Sjakk

inch = 2.54;
rute = 2.2*inch;

/*
for (x = [0:7]) {
    for (y = [0:7]) {
        translate([x*rute,y*rute]) {
            if (((x && 1) && (y && 1))) {
                translate([(x-1)*rute, (y-1)*rute]) {
                    echo(x, y, x && 1);
                    square([rute,rute]);
                }
            }
        }
    }
}
*/

